apiVersion: apps/v1
kind: Deployment
metadata:
  name: tdm-qa-worker
  namespace: automation-tdm-qa
  labels:
    app: tdm-qa
    component: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tdm-qa
      component: worker
  template:
    metadata:
      labels:
        app: tdm-qa
        component: worker
    spec:
      serviceAccountName: automacaoqa
      containers:
        - name: worker
          image: REPLACE_IMAGE_REGISTRY/tdm-qa:latest
          imagePullPolicy: Always
          command: ["node", "server/worker.js"]
          envFrom:
            - configMapRef:
                name: tdm-qa-config
            - secretRef:
                name: tdm-qa-secrets
          resources:
            requests:
              memory: "1Gi"
              cpu: "500m"
            limits:
              memory: "4Gi"
              cpu: "2000m"
